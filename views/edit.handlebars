<div class="row">
    <div class="col-auto">

    </div>
</div>
<div class="row mt-4">
    <div class="col-md-6">
        <h3>Edit:</h3>
        <form  class="form new-blog-form">
            <div class="form-group">
                <label for="blog-name">title:
                </label>
                <textarea name="blog-name" id="blog-name" class="form-input">{{name}}</textarea>
            </div>
            <div class="form-group">
                <label for="blog-desc">content:</label>
                <textarea class="form-input" id="blog-desc" name="blog-desc">{{description}}</textarea>
            </div>
            <div class="form-group">
                <button type= "submit" class="btn btn-sm btn-danger edit" data-id={{id}}>Update</button>
            </div>
        </form>
    </div>
</div>